export const BASE_EXTERNAL_URL = import.meta.env.VITE_BASE_EXTERNAL_URL;

export const DEFAULT_INTERVAL = 60;

export const WEATHER_FORECAST_DAYS = parseFloat(import.meta.env.VITE_WEATHER_FORECAST_DAYS) || 5;